#include <stdio.h>  /* 预处理指令。*/
int main(void){     /* 返回值类型 main（参数void表示空参）函数是起点。 */
    int num;        /* 声明 */
    num = 1;        /* 赋值语句 */
    printf("I'm a sample ");
    printf("computer.\n");
    printf("My favorite number is %d because it is first.", num);

    return 0;
}